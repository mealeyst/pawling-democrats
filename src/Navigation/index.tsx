import styled from 'styled-components';
import { color } from '../theme/color';
import { spacing } from '../theme/spacing';

export const Navigation = styled.nav`
  display: flex;
  color: ${color('secondary.blue1')};
  height: ${spacing(6)}
`